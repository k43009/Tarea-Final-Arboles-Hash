#pragma once


// Lo que se declare aquí pero no se defina, su definición se buscará en el archivo.cpp del mismo nombre.
int Multiplicacion(int a, int b);


// Las funciones se declaran en el .h porque tú puedes declarar muchas veces la misma función sin problemas,
// pero únicamente puede existir una definición, que es la que se va a poner en el .cpp.
